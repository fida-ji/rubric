# GenLayer test configuration.
# Direct-mode tests (tests/direct/) ignore the network and run in-memory.
# Integration tests, if added, run against Testnet Bradbury using ACCOUNT_PRIVATE_KEY from .env.
networks:
  default: testnet_bradbury
  testnet_bradbury:
    accounts:
      - "${ACCOUNT_PRIVATE_KEY}"
    from: "${ACCOUNT_PRIVATE_KEY}"

paths:
  contracts: "contracts"
  artifacts: "artifacts"

environment: .env
